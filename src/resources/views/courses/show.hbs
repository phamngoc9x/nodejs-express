
<div class="mt-4">
  <div class="row">
    <div class="col-md-3">
      <div class="d-flex">
        Trình độ: {{ course.level }}
      </div>
      <button class="btn btn-primary">Học ngay</button>
    </div>
    <div class="col-md-9">
      <h2>{{course.name}}</h2>
      <img src="https://i.ytimg.com/vi/{{course.videoId}}/maxresdefault.jpg" alt="{{ this.name }}" class="w-100 h-auto">
      <p class="mt-2">{{course.description}}</p>
    </div>
  </div>
</div>